language: cpp

sudo: required
dist: xenial

branches:
  only:
    - master

env:
  global:
    - QT_VER=5.12.3

git:
  depth: 175


install: .travis/install.sh
script: .travis/script.sh

jobs:
  include:
  - env: TARGET=x11-static
  - env: TARGET=macos-static

services:
  - xvfb
